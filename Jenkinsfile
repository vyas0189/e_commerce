pipeline {
    agent any

    stages {
        stage("build"){
            steps {
                sh 'node --version'
                echo 'Building...'
            }
        }
        stage("test"){
            steps {
                echo 'Testing...'
            }
        }
    }
}
